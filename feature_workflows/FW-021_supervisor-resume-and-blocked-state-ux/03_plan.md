# Plan

1. Build `next_family_status` from proposal/program/queue state.
2. Add focused API routes and include the status in overview.
3. Render explicit next-family status in the dashboard.
4. Update OpenClaw supervisor summarization and decision wording to use governed states.

Acceptance criteria:

- blocked and active states are obvious and consistent across all surfaces
- supervisor remains in monitor/wait mode when governance says blocked