# Lessons

- once the queue can be intentionally blocked, generic idle language becomes misleading
- OpenClaw should consume the same compact next-family summary as the operator surfaces, not infer intent from queue order alone