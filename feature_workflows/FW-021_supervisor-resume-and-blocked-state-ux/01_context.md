# Context

Once the queue became intentionally governable, blocked state became a valid operating state rather than an error.

Needed change:

- explain whether the queue is blocked pending approval, ready for bootstrap, queued, or actively running

Non-goals:

- additional mutation powers for the supervisor
- autonomous invention of the next family