# FW-010 Lessons

1. Runtime truth was already present in the API; the bigger failure mode was presentation ambiguity.
2. Historical outcomes need explicit labels, otherwise operators will read them as live status when the runtime is active.
