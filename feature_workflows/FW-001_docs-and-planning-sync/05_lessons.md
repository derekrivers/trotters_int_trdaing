# Lessons

## During Implementation

- stable context and active planning need separate homes or the docs layer starts to contradict itself
- top-level README drift is easy to miss after context renames and consolidations

## Durable Takeaways

- keep `context/` stable and move active execution notes into `feature_workflows/`
- phase-level docs should summarize and point to detailed workflow folders instead of duplicating them
