# FW-013 Lessons

1. Once dedicated detail endpoints exist, the overview route should summarize current state instead of dragging historical runtime tables into every operator request.
2. Response size is part of API correctness for operator tooling; field coverage alone is not enough when the runtime database grows quickly.
