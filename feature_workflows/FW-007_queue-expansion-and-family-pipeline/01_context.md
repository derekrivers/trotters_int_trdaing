# Context

## Problem

- the OpenClaw runbook only had two approved items, which means autonomous continuation would stall again after the beta-defensive branch unless the queue was expanded deliberately
- adding many speculative items would create false autonomy, where the supervisor can continue but only into weak or already-discredited ideas

## Current Repo Signals

- `beta_defensive_continuation` is now the active higher-priority follow-up branch
- `momentum_broad_candidate_refine_n4_ms002_rf63` is the remaining unused repo-defined broad-family fallback that is still operationally credible
- `mean_reversion_broad_candidate_n8_ms005_rf21` remains intentionally outside the queue because the roadmap already records it as materially worse

## Non-Goals

- do not reopen the retired `risk + sector` branch
- do not create a large speculative queue with no evidence or stop conditions
- do not create new strategy code just to inflate the queue length
