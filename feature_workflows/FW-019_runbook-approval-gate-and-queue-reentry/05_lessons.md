# Lessons

- approval gating belongs in the queue summary, because that is the shared contract both operator surfaces and OpenClaw can trust
- raw queue order alone is too weak once proposal approval becomes a separate stage