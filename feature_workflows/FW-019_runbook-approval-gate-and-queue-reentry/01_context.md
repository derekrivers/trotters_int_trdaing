# Context

The queue had already been governed against retired and untracked work, but it still needed an explicit approval boundary.

Needed change:

- approval state should control whether a queue item is runnable

Non-goals:

- broader OpenClaw mutation powers
- queue auto-generation