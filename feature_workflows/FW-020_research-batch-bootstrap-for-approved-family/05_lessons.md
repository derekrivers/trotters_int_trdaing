# Lessons

- Windows live-stack writes need atomic-replace retries in every artifact writer, not just the catalog path
- a bounded bootstrap path is safer than manual editing because it keeps plan ids, program ids, and queue entries aligned