# Lessons

- The governed next-family state is not just a secondary panel; the main runtime-health summary must carry it or operators will read a blocked queue as generic idle runtime.
- Blocked-idle tests need fresh worker and service-heartbeat timestamps, otherwise the existing degraded-health rules will correctly mask the governance-specific branch.
