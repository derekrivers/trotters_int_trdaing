# Lessons

- once family comparison becomes a read model, queue governance can consume it directly instead of rereading raw config state
- operator-facing status terms need to be fixed early or later queue logic becomes ambiguous